[package]
name = "crpg-sim"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
blake3 = { workspace = true }
crpg-core = { path = "../crpg-core" }
indexmap = { workspace = true }
serde = { workspace = true }
# Promoted from dev by T008a: state_hash serializes the world through it.
serde_json = { workspace = true }

[dev-dependencies]
# Same as crpg-core (ADR-0006): proptest for the property tests.
proptest.workspace = true
